---
description: "Start (or resume) the Workflow Best Practices track."
allowed-tools: ["Bash(python3:*)"]
---

```bash
python3 .claude/bin/state.py set-current --track workflow
python3 .claude/bin/state.py next
```

Workflow covers: brainstorming, spec-driven dev, plan mode in depth, iterative refinement, TDD with Claude. This is the track that turns a working Claude Code setup into an actually productive one. Confirm and launch.
