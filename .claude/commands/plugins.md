---
description: "Start (or resume) the Plugins track — incl. the superpowers plugin."
allowed-tools: ["Bash(python3:*)"]
---

```bash
python3 .claude/bin/state.py set-current --track plugins
python3 .claude/bin/state.py next
```

Plugins covers: what a plugin actually is, how to install one, a hands-on walkthrough of the `superpowers` plugin (brainstorm → spec → plan → build), and authoring your own. Confirm and launch.
