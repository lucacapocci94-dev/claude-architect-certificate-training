#!/usr/bin/env bash
# PostToolUse hook — currently a no-op stub.
# Reserved for future use (e.g. validating that lesson completions write
# to .claude/state/progress.json). Kept here so settings.json can wire it
# without breaking. Exits 0 silently.
exit 0
